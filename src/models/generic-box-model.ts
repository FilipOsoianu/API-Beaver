export class GenericBoxModel {
  name: string;
  color: string;
  label: string;

  constructor(name: string, color: string, label: string) {
    this.name = name;
    this.color = color;
    this.label = label;
  }
}
